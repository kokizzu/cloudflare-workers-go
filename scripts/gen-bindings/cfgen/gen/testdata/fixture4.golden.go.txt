// Code generated by cfgen from @cloudflare/workers-types@0.0.0-fixture4. DO NOT EDIT.

//go:build js && wasm

// Package matrices is a fourth synthetic fixture package, covering the
// nested-container (array-of-array, record-of-array, array-of-record) loop
// variable naming fix: each nesting level must use a distinct set of
// loop/temp variable names, or an inner loop silently shadows (and
// corrupts) an outer one's.
package matrices

import (
	"syscall/js"

	"github.com/syumai/workers-go/exp/internal/jsrt"
)

// Matrix Matrix exercises nested array/record conversions: a [][]float64 field and a [][]string field (each requiring distinct per-nesting-level loop variable names so an inner loop can't shadow the outer one's index/element - see containerSuffix), plus a Record<string, []string> and an Array<Record<string, number>> to cover the mixed map/array nesting combinations that have the same shadowing hazard.
type Matrix struct {
	Floats [][]float64          `js:"floats"`
	Words  [][]string           `js:"words"`
	Tags   map[string][]string  `js:"tags"`
	Rows   []map[string]float64 `js:"rows"`
}

func matrixFromJS(v js.Value) (Matrix, error) {
	var out Matrix
	{
		out.Floats = make([][]float64, v.Get("floats").Length())
		for i := range out.Floats {
			out.Floats[i] = make([]float64, v.Get("floats").Index(i).Length())
			for i1 := range out.Floats[i] {
				out.Floats[i][i1] = v.Get("floats").Index(i).Index(i1).Float()
			}
		}
	}
	{
		out.Words = make([][]string, v.Get("words").Length())
		for i := range out.Words {
			out.Words[i] = make([]string, v.Get("words").Index(i).Length())
			for i1 := range out.Words[i] {
				out.Words[i][i1] = v.Get("words").Index(i).Index(i1).String()
			}
		}
	}
	{
		out.Tags = make(map[string][]string)
		keys := js.Global().Get("Object").Call("keys", v.Get("tags"))
		for i := 0; i < keys.Length(); i++ {
			k := keys.Index(i).String()
			var vv []string
			vv = make([]string, v.Get("tags").Get(k).Length())
			for i1 := range vv {
				vv[i1] = v.Get("tags").Get(k).Index(i1).String()
			}
			out.Tags[k] = vv
		}
	}
	{
		out.Rows = make([]map[string]float64, v.Get("rows").Length())
		for i := range out.Rows {
			out.Rows[i] = make(map[string]float64)
			keys1 := js.Global().Get("Object").Call("keys", v.Get("rows").Index(i))
			for i1 := 0; i1 < keys1.Length(); i1++ {
				k1 := keys1.Index(i1).String()
				var vv1 float64
				vv1 = v.Get("rows").Index(i).Get(k1).Float()
				out.Rows[i][k1] = vv1
			}
		}
	}
	return out, nil
}

func (o Matrix) toJS() js.Value {
	obj := jsrt.NewObject()
	if len(o.Floats) > 0 {
		arr := js.Global().Get("Array").New(len(o.Floats))
		for i, e := range o.Floats {
			arr1 := js.Global().Get("Array").New(len(e))
			for i1, e1 := range e {
				arr1.SetIndex(i1, e1)
			}
			arr.SetIndex(i, arr1)
		}
		obj.Set("floats", arr)
	}
	if len(o.Words) > 0 {
		arr := js.Global().Get("Array").New(len(o.Words))
		for i, e := range o.Words {
			arr1 := js.Global().Get("Array").New(len(e))
			for i1, e1 := range e {
				arr1.SetIndex(i1, e1)
			}
			arr.SetIndex(i, arr1)
		}
		obj.Set("words", arr)
	}
	if len(o.Tags) > 0 {
		m := jsrt.NewObject()
		for k, v := range o.Tags {
			arr1 := js.Global().Get("Array").New(len(v))
			for i1, e1 := range v {
				arr1.SetIndex(i1, e1)
			}
			m.Set(k, arr1)
		}
		obj.Set("tags", m)
	}
	if len(o.Rows) > 0 {
		arr := js.Global().Get("Array").New(len(o.Rows))
		for i, e := range o.Rows {
			m1 := jsrt.NewObject()
			for k1, v1 := range e {
				m1.Set(k1, v1)
			}
			arr.SetIndex(i, m1)
		}
		obj.Set("rows", arr)
	}
	return obj
}
