// Code generated by cfgen from @cloudflare/workers-types@0.0.0-fixture5. DO NOT EDIT.

//go:build js && wasm

// Package registry is a fifth synthetic fixture package, covering the
// tmp/06-codegen-spec.md 4.1 cfgen extensions: Map<string, T> return values
// and rest parameters.
package registry

import (
	"syscall/js"

	"github.com/syumai/workers-go/exp/internal/jsrt"
)

// Registry Registry exercises the tmp/06-codegen-spec.md 4.1 cfgen extensions: a Map<string, T>-returning method (with a resolvable value type, and with an unresolvable typeParam value type falling back to js.Value) and rest parameters (an any-typed one, spread straight into jsrt.Call, and a string-typed one, converted element by element).
type Registry struct{ v js.Value }

// RegistryFromJS wraps a JS Registry value.
func RegistryFromJS(v js.Value) *Registry { return &Registry{v: v} }

// JSValue returns the underlying JS value.
func (x *Registry) JSValue() js.Value { return x.v }

// NewRegistry resolves the binding named bindingName from env.
func NewRegistry(bindingName string) (*Registry, error) {
	v, err := jsrt.Binding(bindingName)
	if err != nil {
		return nil, err
	}
	return &Registry{v: v}, nil
}

// GetAll a Map<string, string> return becomes map[string]string
func (x *Registry) GetAll() (map[string]string, error) {
	p, err := jsrt.Call(x.v, "getAll")
	if err != nil {
		return nil, err
	}
	r, err := jsrt.Await(p)
	if err != nil {
		return nil, err
	}
	var ret map[string]string
	ret = make(map[string]string)
	mapKeys := js.Global().Get("Array").Call("from", r.Call("keys"))
	for mapIdx := 0; mapIdx < mapKeys.Length(); mapIdx++ {
		mapKey := mapKeys.Index(mapIdx).String()
		var mapVal string
		mapVal = r.Call("get", mapKey).String()
		ret[mapKey] = mapVal
	}
	return ret, nil
}

// GetAllRaw a Map<string, T> return with an unresolved typeParam value falls back to map[string]js.Value
func (x *Registry) GetAllRaw() (map[string]js.Value, error) {
	p, err := jsrt.Call(x.v, "getAllRaw")
	if err != nil {
		return nil, err
	}
	r, err := jsrt.Await(p)
	if err != nil {
		return nil, err
	}
	var ret map[string]js.Value
	ret = make(map[string]js.Value)
	mapKeys := js.Global().Get("Array").Call("from", r.Call("keys"))
	for mapIdx := 0; mapIdx < mapKeys.Length(); mapIdx++ {
		mapKey := mapKeys.Index(mapIdx).String()
		var mapVal js.Value
		mapVal = r.Call("get", mapKey)
		ret[mapKey] = mapVal
	}
	return ret, nil
}

// Log a rest parameter whose element type is any becomes a Go ...any spread directly into jsrt.Call
func (x *Registry) Log(level string, args ...any) error {
	_, err := jsrt.Call(x.v, "log", append([]any{level}, args...)...)
	return err
}

// Tag a rest parameter whose element type resolves to a concrete Go type (string) is converted element by element into a []any before the call
func (x *Registry) Tag(name string, values ...string) error {
	arg1 := make([]any, len(values))
	for i, e := range values {
		arg1[i] = e
	}
	_, err := jsrt.Call(x.v, "tag", append([]any{name}, arg1...)...)
	return err
}
