// Code generated by cfgen from @cloudflare/workers-types@0.0.0-fixture3. DO NOT EDIT.

//go:build js && wasm

// Package streams is a third synthetic fixture package, covering the stream
// type mappings added by tmp/06-codegen-spec.md 3.1.1: WritableStream and
// ReadableStream at property/return position, ReadableStream and
// WritableStream at parameter position, and a types: override forcing
// io.Reader on an otherwise-unresolvable union parameter.
package streams

import (
	"io"
	"syscall/js"

	"github.com/syumai/workers-go/exp/internal/jsrt"
)

// Streams Streams exercises the stream type mappings from tmp/06-codegen-spec.md 3.1.1: a WritableStream property/return maps to io.WriteCloser, a ReadableStream property/return maps to io.ReadCloser (unchanged from before 3.1.1), a ReadableStream parameter maps to io.Reader (via jsrt.ReadableStreamFromReader), a WritableStream parameter falls back to js.Value for now, and a types: override can force io.Reader on an otherwise-unresolvable union parameter.
type Streams struct{ v js.Value }

// StreamsFromJS wraps a JS Streams value.
func StreamsFromJS(v js.Value) *Streams { return &Streams{v: v} }

// JSValue returns the underlying JS value.
func (x *Streams) JSValue() js.Value { return x.v }

// NewStreams resolves the binding named bindingName from env.
func NewStreams(bindingName string) (*Streams, error) {
	v, err := jsrt.Binding(bindingName)
	if err != nil {
		return nil, err
	}
	return &Streams{v: v}, nil
}

// Sink
func (x *Streams) Sink() io.WriteCloser {
	var ret io.WriteCloser
	ret = jsrt.WriteCloser(x.v.Get("sink"))
	return ret
}

// Download returns a ReadableStream; property/return position is unaffected by the parameter-position change
func (x *Streams) Download() (io.ReadCloser, error) {
	p, err := jsrt.Call(x.v, "download")
	if err != nil {
		return nil, err
	}
	r, err := jsrt.Await(p)
	if err != nil {
		return nil, err
	}
	var ret io.ReadCloser
	ret = jsrt.ReadCloser(r)
	return ret, nil
}

// Upload a ReadableStream parameter maps to io.Reader
func (x *Streams) Upload(body io.Reader) error {
	p, err := jsrt.Call(x.v, "upload", jsrt.ReadableStreamFromReader(body))
	if err != nil {
		return err
	}
	_, err = jsrt.Await(p)
	return err
}

// Attach a WritableStream parameter falls back to js.Value for now
func (x *Streams) Attach(dest js.Value) error {
	_, err := jsrt.Call(x.v, "attach", dest)
	return err
}

// UploadRaw an otherwise-unresolvable union parameter forced to io.Reader via a types: override
func (x *Streams) UploadRaw(data io.Reader) error {
	p, err := jsrt.Call(x.v, "uploadRaw", jsrt.ReadableStreamFromReader(data))
	if err != nil {
		return err
	}
	_, err = jsrt.Await(p)
	return err
}
